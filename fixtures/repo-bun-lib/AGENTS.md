# acme-lib

A small utility library.

## Conventions

- Use `bun test` to run the test suite.
- Do not edit files under `src/proto/` — they are generated from `.proto` schemas.
- Prefer named exports over default exports in this codebase.
- Never commit directly to `main`; always open a PR.
